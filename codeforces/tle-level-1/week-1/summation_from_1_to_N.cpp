#include <bits/stdc++.h>
using namespace std;

void solve() {
  long long N;
  cin >> N;

  cout << (N * 1ll * (N+1))/2;
}

int32_t main() {
  ios::sync_with_stdio(false);
  cin.tie(0);

  solve();

  return 0;
}
